export interface Theme {
    /** Zero-based index of the theme record. */
    id: number;
    /** Name of the theme. */
    name: string;
    /** Standard Alpha Red Green Blue color value. */
    val: string;
}